// Unified site header. The navigation is the BanzamiNav component (header +
// desktop mega menu + mobile accordion), faithful to HANDOFF_Banzami_Nav.md.
// Kept as `SiteHeader` so every page keeps importing the same entry point.
export { BanzamiNav as SiteHeader } from './BanzamiNav';
