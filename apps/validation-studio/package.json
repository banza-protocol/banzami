{
  "name": "validation-studio",
  "version": "1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "next dev --port 3099 --hostname localhost",
    "build": "echo 'Validation Studio is local-only — production build is disabled.' && exit 1",
    "start": "echo 'Validation Studio is local-only — production start is disabled.' && exit 1",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "next": "^15.5.25",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.28",
    "tailwindcss": "^3.4.14",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
